# 工具链锁定版本(唯一来源)。升级走 change 分支 + build-all 全绿后合并。
# Xcode 版本另见 .xcode-version。

[tools]
tuist = "4.203.1"
node = "24.19.0"                      # openspec CLI 的运行时
"npm:@fission-ai/openspec" = "1.8.0"  # openspec CLI(各 store 流程层,ticket 06 起用)
