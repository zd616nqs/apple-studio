schema: full-change

context: |
  DemoPhotoMark is the retained Swift and Objective-C interoperability example App.
  Its behavior vocabulary is defined in ../CONTEXT.md and current target facts in ../Project.swift.

operations:
  archive:
    guidance:
      - "GATE-REQUIRED-VERIFICATION — ../../../GOVERNANCE.md"
      - "CONV-OPENSPEC-ARCHIVE — ../../../GOVERNANCE.md"
