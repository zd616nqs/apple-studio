schema: full-change

context: |
  Modules is the shared layer consumed by one or more Apps; public behavior can affect every dependent App.
  Current target facts are defined in ../Project.swift and behavior contracts live in this store.

operations:
  archive:
    guidance:
      - "GATE-REQUIRED-VERIFICATION — ../../GOVERNANCE.md"
      - "CONV-OPENSPEC-ARCHIVE — ../../GOVERNANCE.md"
